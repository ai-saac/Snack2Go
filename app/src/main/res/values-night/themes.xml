<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Snack2Go" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="android:statusBarColor">@android:color/background_dark</item>
        <item name="backgroundColor">@color/background</item>
        <item name="android:popupMenuStyle">@style/PopupMenu</item>
    </style>
    <style name="Theme.Snack2Go" parent="Base.Theme.Snack2Go">
        <item name="android:windowBackground">@drawable/splashscreen_night</item>>
    </style>
    <style name="PopupMenu" parent="Widget.AppCompat.PopupMenu">
        <!-- Cambia el color de fondo del menú emergente -->
        <item name="android:background">@color/background</item>
        <!-- Cambia el color del texto del menú emergente -->
        <item name="android:textColor">@color/gris_natural</item>
        <!-- Otros atributos de estilo según sea necesario -->
    </style>
</resources>